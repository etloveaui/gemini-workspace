⚡ Ran command cmd /c '"git" config i18n.filesEncoding utf-8 & "git" co
  - `tasks.py`: `invoke review` 출력 경로에 안전 출력 레이어 추가      
    - Rich 출력이 cp949 등에서 실패 시 안전하게 대체 출력(문자 치환)으 
로 폴백.
- 제안 엔진 관련
  - `scripts/auto_update/` 아카이브로 이동(시스템 충돌/노이즈 최소화 목
적).
  - `invoke auto.scan/auto.propose`는 현재 비활성(폴더 이동으로 차단). 

**변경 미리보기 요약**
- 수정: `.agents/config.json`, `tasks.py`, `scripts/auto_update/scanner.py`
- 이동(아카이브): `config.json` → `scratchpad/_archive/config.json`    
- 아카이브: `scripts/_archive/*`에 위 나열된 파일/폴더
- 검증: `invoke review` 인코딩 에러 없이 동작(출력은 안전치환 적용)    

**진행 단계별 대응 상태**
- 1단계(스크립트 대폭 정리): 위험도 높은 삭제 대신 아카이브로 이동(롤백
 가능),
중복/실험성 스크립트 우선 정리 완료.
- 2단계(Git/UTF-8 강제): 레포 로컬 설정으로 안전 적용. 시스템 전역 `setx`, `chcp
`는 보류(원하시면 승인 후 실행).
- 3단계(깨진 문서 정리): 파일 자체의 한글 손상은 “재작성/정정”이 필요합
니다. 대
량 수동 수정은 리스크가 커서, 우선 대상 목록부터 확정 요청드립니다.    
- 4단계(시스템 검증): `invoke review` 정상, Git 상태/디프 확인 가능. 추
후 `pytes
t -q`와 간단 출력 테스트 가능.

**권장 다음 액션**
- 커밋: `venv\Scripts\python -m invoke git.commit-safe -m "chore(utf8): set agen
t=codex, archive unstable scripts, add safe console printing, set repo 
UTF-8 git
 config"`
- 추가 조치 선택
  - 전역 환경/코드페이지 적용: `setx PYTHONIOENCODING utf-8`, `setx PYTHONLEGACY
WINDOWSFSENCODING utf-8`, `chcp 65001` 실행 여부 승인 필요.
  - 문서 복구 범위 지정: `GEMINI.md`, `docs/HUB.md` 등 “깨짐” 재작성/정
정 진행.
  - `invoke` 출력 경로 전반에 동일 안전 레이어 확대 여부(현재 review에 
우선 적용
).

진행 승인 주시면 커밋과(또는 전역 설정 적용), 깨진 문서 복구 작업까지  
이어가겠습
니다.