# --- Python / OS common ---
__pycache__/
*.py[cod]
*$py.class
*.so
*.dll
.venv/
venv/
.env
.env.*

# --- Windows problematic files ---
nul
NUL
*.tmp
*.temp
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage*
htmlcov/
.DS_Store
Thumbs.db
.vscode/
.idea/

# --- Project specifics ---
# (1) Block all under .gemini, allow only context_policy.yaml
.gemini/**
!.gemini/context_policy.yaml

# (2) Local-only project space (never commit)
projects/
secrets/

# (3) Local artifacts (enable as needed)
usage.db*
*.sqlite
*.db
*.log
*.tmp

# Block local CLAUDE configs
.claude*/

# Environment sync files (ZIP packages)
sync_package/*.zip
environment_sync_*.zip

